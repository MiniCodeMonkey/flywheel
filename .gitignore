# Build output
/flywheel
/dist/
*.test

# Local config & secrets (also lives in ~/.config/flywheel, outside repo)
/.config/
*.token

# Scratch
/scratch/

# This repo tracks its design docs (global ignore excludes docs/superpowers)
!docs/superpowers/
